name: josh_security
description: "Proyecto Centinela - Aplicación de Ciberseguridad Global"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.6.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Inventario de Activos de JOSH Security
  assets:
    - assets/certificates/api_cert.pem
    - assets/logo.png